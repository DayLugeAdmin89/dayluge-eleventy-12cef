<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>The DayLuge</title>
  <style>
    body {
      font-family: Courier, monospace;
      margin: 40px;
      line-height: 1.5;
    }
    h1 {
      text-align: center;
      font-size: 28px;
      margin-bottom: 30px;
    }
    .grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 20px;
    }
    a {
      text-decoration: none;
      font-size: 13pt;
    }
    .news { color: red; }
    .editorial { color: orange; }
    .sports { color: blue; }
    .funny { color: green; }
    .culture { color: purple; }
    .longform { color: brown; }
  </style>
</head>
<body>
  <h1>THE DAYLUGE</h1>
  <hr>
  <div class="grid">
    {{ content | safe }}
  </div>
</body>
</html>

